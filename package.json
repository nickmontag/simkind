{
  "name": "living-character-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Engine-authoritative runtime primitives for goal-driven LLM characters",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "NOTICE.md", "docs"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "check": "npm run build && npm run typecheck && npm test",
    "test": "vitest run",
    "example": "tsx examples/reference-world.ts",
    "example:station": "tsx examples/station-world.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
