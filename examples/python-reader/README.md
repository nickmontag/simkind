# Independent Python document reader

See [Interoperability](../../docs/interoperability.md) for installation, commands,
fixture scope, and explicit compatibility limits. This reader never invokes the
TypeScript implementation, launches a host, or calls a provider. Its only Python
dependency is the pinned jsonschema package and its transitive dependencies.
