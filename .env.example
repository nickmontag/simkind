# Create an OpenRouter key at https://openrouter.ai/settings/keys
# Select a model ID at https://openrouter.ai/models (structured outputs required).
# Fill both values in .env. No model is selected by default.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
